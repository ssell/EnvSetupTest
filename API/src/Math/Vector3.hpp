#pragma once

#include <cmath>

namespace API
{
    template<typename T>
    class Vector3 
    {
    public:

        Vector3();
        ~Vector3();

    protected:

    private:
    };
}

#include "Vector3.inl"